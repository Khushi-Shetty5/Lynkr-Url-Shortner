<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>URL Analytics</title>
</head>
<body>
   <h1>URL Analytics</h1>
   <p><strong>Short ID:</strong> <%= shortId %></p>
   <p><strong>Total Clicks:</strong> <%= totalClicks %></p>
   <h2>Visit History</h2>
   <% if (analytics.length > 0) { %>
       <ul>
           <% analytics.forEach((visit, index) => { %>
               <li><%= new Date(visit.timestamp).toLocaleString() %></li>
           <% }) %>
       </ul>
   <% } else { %>
       <p>No visits yet.</p>
   <% } %>
   <a href="/">Go Back</a>
</body>
</html>
