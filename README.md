# URL-SHORTNER
URL Shortener Project: This is a simple URL Shortener web application built with Node.js, Express, and MongoDB. The project allows users to shorten long URLs and access them through a shortened link, which redirects to the original URL.
